{
  "name": "fintech",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "dev:api": "pnpm --filter api dev",
    "dev:worker": "pnpm --filter worker dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r run lint --parallel",
    "test": "vitest run"
  },
  "devDependencies": {
    "@fastify/cors": "^10.0.0",
    "drizzle-orm": "^0.38.4",
    "pg": "^8.18.0",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
